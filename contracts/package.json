{
  "name": "ecoxchange-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "EcoXchange demo distribution contracts for Base Sepolia (TESTNET ONLY)",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:base-sepolia": "hardhat run scripts/deploy-demo.ts --network baseSepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^3.0.0",
    "@openzeppelin/contracts": "^5.1.0",
    "hardhat": "^2.22.0",
    "typescript": "^5.5.0",
    "viem": "^2.21.0"
  }
}
