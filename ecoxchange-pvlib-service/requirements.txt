# pvlib unified on >=0.15 across the merged service (Engine A requires PSM4 /
# get_nsrdb_psm4_aggregated). The canonical engine itself
# (ecoxchange-verification-engine) is installed separately from
# ../verification-engine — see the Dockerfile, or run
# `pip install -e ../verification-engine` for local dev.
pvlib>=0.15
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pandas>=2.2,<3
numpy>=1.26,<2
httpx==0.28.1
pytest==8.3.4
pytest-asyncio==0.25.0
