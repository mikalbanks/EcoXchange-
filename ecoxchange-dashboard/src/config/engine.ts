// Single source of truth for the verification-engine version shown in the
// UI. The engine itself (verification-engine/, pvlib ModelChain) declares
// __version__ = "2.0.0"; the dashboard displays it with the v prefix.
export const ENGINE_VERSION = "v2.0.0";
