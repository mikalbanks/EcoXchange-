# LIVE REG D 506(c) OFFERING BUILD — DO NOT USE WITHOUT SECURITIES COUNSEL SIGN-OFF.
# The app REFUSES TO START in live mode unless VITE_COUNSEL_APPROVED=true is set
# explicitly (startup guard in src/main.tsx). Counsel must review every string in
# src/compliance/config/bannerConfig.ts and disclaimerConfig.ts first — the live
# blocks are intentionally [PLACEHOLDER] until then.
VITE_COMPLIANCE_MODE=live
VITE_ACCREDITATION_GATE=true
# VITE_COUNSEL_APPROVED=true   # uncomment ONLY after counsel review
