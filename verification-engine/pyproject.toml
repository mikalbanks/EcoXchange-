[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ecoxchange-verification-engine"
version = "2.3.0"
description = "EcoXchange production-verification engine (pvlib PVWatts ModelChain, P50/P90, loss waterfall, reconciliation)."
requires-python = ">=3.11"
dependencies = [
    "pvlib==0.15.2",
    "pandas>=2.0",
    "numpy>=1.24",
    "scipy>=1.10",
    "pyyaml>=6.0",
    "requests>=2.31",
    # Spec 21 ingestion layer (src/ingestion/).
    "pvanalytics==0.2.2",
    "ruptures>=1.1",
    "s3fs>=2023.1",
    "pyarrow>=14.0",
    # Spec 22 analytics layer (src/analytics/). Pinned: §2's verified API surface
    # is 3.2.1's, and the YoY confidence-interval default this module overrides
    # is a library default, not a documented contract.
    "rdtools==3.2.1",
]

[project.optional-dependencies]
# Only needed for the EIA fleet re-validation harness (.xlsx parsing).
fleet = ["openpyxl>=3.1"]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
include = ["verification_engine*", "ingestion*", "analytics*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
